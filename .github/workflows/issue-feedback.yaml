name: Issue feedback (supplied default configuration)

on:
  issues:
    types:
      - opened
      - reopened

permissions:
  contents: read
  issues: write

jobs:
  feedback:
    uses: SecondSkoll/generic-agentic-workflows/.github/workflows/opencode-issue-feedback.yml@61ed1bbd34a878f3ae270b1e4ff027cf786b730b
    permissions:
      contents: read
      issues: write
    with:
      configuration_source: default
      configuration_ref: 61ed1bbd34a878f3ae270b1e4ff027cf786b730b
      configuration_profile: issue-feedback
      focus: general
      max_issues: 20
      validate_only: true
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
